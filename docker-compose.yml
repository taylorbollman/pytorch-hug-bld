version: '3'
services:
  tb-dev-container:  # Rename to something more descriptive if needed
    build: .
    volumes:
      - .:/code
    env_file:
      - .env  # If you're using environment variables, put them in a file named .env
